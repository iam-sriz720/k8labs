apiVersion: v1
kind: Pod
metadata:
  name: my-pod
  labels:
    app: my-app
    type: development
spec:
  containers:
    - name: web01
      image: nginx:latest
      ports:
        - containerPort: 80
