apiVersion: v1
kind: Service
metadata:
  name: sample-app-nodeport-service
  labels:
    app: sample-app
spec:
  type: NodePort
  selector: 
    app: sample-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30007
